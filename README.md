# Repository Name

## About
Describe why this project exists and what it accomplishes.

## Getting Started

### Dependencies

This is an example of how to list things you need to use the software and how to install them.
* For pip dependencies:

	  pip3 install -r requirements.txt

* For ros dependencies:

	  rosdep install --from-paths src --ignore-src -r -y

### Build Instructions
Mention specific build or compile instructions.<br/>
For instance, if using `catkin`, `colcon` or `docker`, mention the commands here.

### Running the Project
How to launch the project and get expected outcomes.<br/>
This includes specific `roslaunch` or `docker run` commands.

### Parameter Configuration
Specify what parameters can be changed and how. <br/>
This can include a world file, a map file, the robot model, start/goal poses, etc. Depends on the project scope.

## API Documentation

Include all major APIs and scripts documentation with a brief description of each.

For instance, if following a ROS repository structure, mention the important launch files, nodes, topics, services, and parameters. Attaching an example below:

* **Published Topics**

  * **`/imu`** ([sensor_msgs/Imu](http://docs.ros.org/api/sensor_msgs/html/msg/Imu.html)) \
	&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;Filtered imu values with fused quaternions from the madgwick filter.


## Images/GIFs (Optional):
Associated with the successful execution of the project. Do not upload images/gifs in the repository, instead generate a link using the Issues section.

## References (Optional):
Any important resources utilized that contribute to the project.
